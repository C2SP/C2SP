# Generated by "go generate ./policy". DO NOT EDIT.

policy:
  approval:
    - or:
      - the stewards have approved
      - and:
        - or:
          - the age maintainers have authored relevant changes
          - the age maintainers have approved relevant changes
        - or:
          - the age-plugin maintainers have authored relevant changes
          - the age-plugin maintainers have approved relevant changes
        - or:
          - the BLAKE3 maintainers have authored relevant changes
          - the BLAKE3 maintainers have approved relevant changes
        - or:
          - the chacha20-poly1305-siv maintainers have authored relevant changes
          - the chacha20-poly1305-siv maintainers have approved relevant changes
        - or:
          - the chacha8rand maintainers have authored relevant changes
          - the chacha8rand maintainers have approved relevant changes
        - or:
          - the det-keygen maintainers have authored relevant changes
          - the det-keygen maintainers have approved relevant changes
        - or:
          - the https-bastion maintainers have authored relevant changes
          - the https-bastion maintainers have approved relevant changes
        - or:
          - the jq255 maintainers have authored relevant changes
          - the jq255 maintainers have approved relevant changes
        - or:
          - the signed-note maintainers have authored relevant changes
          - the signed-note maintainers have approved relevant changes
        - or:
          - the static-ct-api maintainers have authored relevant changes
          - the static-ct-api maintainers have approved relevant changes
        - or:
          - the tlog-checkpoint maintainers have authored relevant changes
          - the tlog-checkpoint maintainers have approved relevant changes
        - or:
          - the tlog-cosignature maintainers have authored relevant changes
          - the tlog-cosignature maintainers have approved relevant changes
        - or:
          - the tlog-mirror maintainers have authored relevant changes
          - the tlog-mirror maintainers have approved relevant changes
        - or:
          - the tlog-tiles maintainers have authored relevant changes
          - the tlog-tiles maintainers have approved relevant changes
        - or:
          - the tlog-witness maintainers have authored relevant changes
          - the tlog-witness maintainers have approved relevant changes
        - or:
          - the vrf-r255 maintainers have authored relevant changes
          - the vrf-r255 maintainers have approved relevant changes
        - or:
          - the XAES-256-GCM maintainers have authored relevant changes
          - the XAES-256-GCM maintainers have approved relevant changes
  disapproval:
    requires: # stewards can block PRs
      teams:
        - "C2SP/stewards"

approval_rules:
  - name: the stewards have approved
    requires:
      count: 1
      teams:
        - "C2SP/stewards"


  - name: the age maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^age\\.md$"
          - "^age/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/age"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the age maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^age\\.md$"
          - "^age/.*$"
      has_author_in:
        teams:
          - "C2SP/age"
    requires:
      count: 0

  - name: the age-plugin maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^age-plugin\\.md$"
          - "^age-plugin/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/age-plugin"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the age-plugin maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^age-plugin\\.md$"
          - "^age-plugin/.*$"
      has_author_in:
        teams:
          - "C2SP/age-plugin"
    requires:
      count: 0

  - name: the BLAKE3 maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^BLAKE3\\.md$"
          - "^BLAKE3/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/BLAKE3"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the BLAKE3 maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^BLAKE3\\.md$"
          - "^BLAKE3/.*$"
      has_author_in:
        teams:
          - "C2SP/BLAKE3"
    requires:
      count: 0

  - name: the chacha20-poly1305-siv maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^chacha20-poly1305-siv\\.md$"
          - "^chacha20-poly1305-siv/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/chacha20-poly1305-siv"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the chacha20-poly1305-siv maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^chacha20-poly1305-siv\\.md$"
          - "^chacha20-poly1305-siv/.*$"
      has_author_in:
        teams:
          - "C2SP/chacha20-poly1305-siv"
    requires:
      count: 0

  - name: the chacha8rand maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^chacha8rand\\.md$"
          - "^chacha8rand/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/chacha8rand"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the chacha8rand maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^chacha8rand\\.md$"
          - "^chacha8rand/.*$"
      has_author_in:
        teams:
          - "C2SP/chacha8rand"
    requires:
      count: 0

  - name: the det-keygen maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^det-keygen\\.md$"
          - "^det-keygen/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/det-keygen"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the det-keygen maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^det-keygen\\.md$"
          - "^det-keygen/.*$"
      has_author_in:
        teams:
          - "C2SP/det-keygen"
    requires:
      count: 0

  - name: the https-bastion maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^https-bastion\\.md$"
          - "^https-bastion/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/https-bastion"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the https-bastion maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^https-bastion\\.md$"
          - "^https-bastion/.*$"
      has_author_in:
        teams:
          - "C2SP/https-bastion"
    requires:
      count: 0

  - name: the jq255 maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^jq255\\.md$"
          - "^jq255/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/jq255"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the jq255 maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^jq255\\.md$"
          - "^jq255/.*$"
      has_author_in:
        teams:
          - "C2SP/jq255"
    requires:
      count: 0

  - name: the signed-note maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^signed-note\\.md$"
          - "^signed-note/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/signed-note"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the signed-note maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^signed-note\\.md$"
          - "^signed-note/.*$"
      has_author_in:
        teams:
          - "C2SP/signed-note"
    requires:
      count: 0

  - name: the static-ct-api maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^static-ct-api\\.md$"
          - "^static-ct-api/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/static-ct-api"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the static-ct-api maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^static-ct-api\\.md$"
          - "^static-ct-api/.*$"
      has_author_in:
        teams:
          - "C2SP/static-ct-api"
    requires:
      count: 0

  - name: the tlog-checkpoint maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^tlog-checkpoint\\.md$"
          - "^tlog-checkpoint/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/tlog-checkpoint"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the tlog-checkpoint maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^tlog-checkpoint\\.md$"
          - "^tlog-checkpoint/.*$"
      has_author_in:
        teams:
          - "C2SP/tlog-checkpoint"
    requires:
      count: 0

  - name: the tlog-cosignature maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^tlog-cosignature\\.md$"
          - "^tlog-cosignature/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/tlog-cosignature"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the tlog-cosignature maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^tlog-cosignature\\.md$"
          - "^tlog-cosignature/.*$"
      has_author_in:
        teams:
          - "C2SP/tlog-cosignature"
    requires:
      count: 0

  - name: the tlog-mirror maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^tlog-mirror\\.md$"
          - "^tlog-mirror/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/tlog-mirror"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the tlog-mirror maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^tlog-mirror\\.md$"
          - "^tlog-mirror/.*$"
      has_author_in:
        teams:
          - "C2SP/tlog-mirror"
    requires:
      count: 0

  - name: the tlog-tiles maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^tlog-tiles\\.md$"
          - "^tlog-tiles/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/tlog-tiles"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the tlog-tiles maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^tlog-tiles\\.md$"
          - "^tlog-tiles/.*$"
      has_author_in:
        teams:
          - "C2SP/tlog-tiles"
    requires:
      count: 0

  - name: the tlog-witness maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^tlog-witness\\.md$"
          - "^tlog-witness/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/tlog-witness"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the tlog-witness maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^tlog-witness\\.md$"
          - "^tlog-witness/.*$"
      has_author_in:
        teams:
          - "C2SP/tlog-witness"
    requires:
      count: 0

  - name: the vrf-r255 maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^vrf-r255\\.md$"
          - "^vrf-r255/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/vrf-r255"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the vrf-r255 maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^vrf-r255\\.md$"
          - "^vrf-r255/.*$"
      has_author_in:
        teams:
          - "C2SP/vrf-r255"
    requires:
      count: 0

  - name: the XAES-256-GCM maintainers have approved relevant changes
    if:
      changed_files:
        paths:
          - "^XAES-256-GCM\\.md$"
          - "^XAES-256-GCM/.*$"
    requires:
      count: 1
      teams:
        - "C2SP/XAES-256-GCM"
    options:
      allow_contributor: true
      methods:
        comments: [] # disable comment-based approval

  - name: the XAES-256-GCM maintainers have authored relevant changes
    if:
      changed_files:
        paths:
          - "^XAES-256-GCM\\.md$"
          - "^XAES-256-GCM/.*$"
      has_author_in:
        teams:
          - "C2SP/XAES-256-GCM"
    requires:
      count: 0

